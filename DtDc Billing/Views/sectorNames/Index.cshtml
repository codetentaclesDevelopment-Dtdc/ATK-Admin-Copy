@model IEnumerable<DtDc_Billing.Entity_FR.sectorName>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.sname)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.sname)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.sname_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.sname_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.sname_id })
        </td>
    </tr>
}

</table>
